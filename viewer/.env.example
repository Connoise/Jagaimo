# Supabase REST endpoint + anon key (RLS-constrained — see core/db/schema.sql).
# The anon key only reaches the host over the tailnet; RLS is defense-in-depth.
VITE_SUPABASE_URL=http://your-supabase-host:8000
VITE_SUPABASE_ANON_KEY=your-anon-key
